# =============================================================================
# Sorachio-STS Python Dependencies
# Install with: pip install -r requirements.txt
# =============================================================================

# --- Core async & HTTP ---
httpx>=0.27.0
aiohttp>=3.9.0
aiofiles>=23.2.0

# --- Configuration ---
pyyaml>=6.0.1
pydantic>=2.6.0
pydantic-settings>=2.2.0

# --- Audio Capture & Playback ---
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.26.0

# --- Voice Activity Detection ---
webrtcvad-wheels>=2.0.14      # webrtcvad pre-built for Windows

# --- TTS: Kokoro ---
kokoro>=0.9.2
# Kokoro dependencies (usually pulled automatically):
# torch, torchaudio, transformers, phonemizer, etc.

# --- Utilities ---
rich>=13.7.0                  # Beautiful terminal output
typer>=0.12.0                 # CLI framework
python-dotenv>=1.0.0
structlog>=24.1.0             # Structured logging

# --- Data & Storage ---
# (no heavy DB yet — pure JSON LTM for v0.1)

# --- Development ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
